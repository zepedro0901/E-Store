# Override to layer on top of anycubic-photon-m5s.ini when slicing files that
# already have supports baked into the mesh (e.g. Lychee/ChiTuBox "PRESUPPORTED"
# exports) -- prevents PrusaSlicer from generating a second, redundant support tree.
supports_enable = 0
pad_enable = 0
